CREATE TABLE venue_area (
    venue_area_id SERIAL PRIMARY KEY,
    venue_area_name TEXT NOT NULL
);

ALTER TABLE venue
ADD COLUMN venue_area_id INTEGER;

ALTER TABLE venue
ADD CONSTRAINT venue_venue_area_id_fkey
FOREIGN KEY (venue_area_id)
REFERENCES venue_area(venue_area_id);
